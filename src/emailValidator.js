const axios = require('axios');

$url = 'https://emailverification.whoisxmlapi.com/api/v1?apiKey=&emailAddress='. $email;
return file_get_contents($url);

if($emailValidation['dnsCheck'] == 'false' || $emailValidation['disposableCheck'] == 'true') 
{
  echo $heikeApiWrapper->sendError('El email no es valido. Por favor prueba con otro email');